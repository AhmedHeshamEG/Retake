fastapi==0.139.0
uvicorn==0.51.0
python-multipart==0.0.32
faster-whisper==1.2.1
sentence-transformers==5.6.0
rapidfuzz==3.14.5
imageio-ffmpeg==0.6.0
# Model Context Protocol server (retake_mcp.py). Lets Claude -- or any other MCP
# client -- drive Retake conversationally. The editor itself works without it.
mcp>=2.1.1
# Self-signed LAN certificate, so a phone can keep its screen awake while it
# uploads. Without it Retake serves plain HTTP and says so.
cryptography>=42.0
