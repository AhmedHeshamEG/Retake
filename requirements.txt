fastapi==0.139.0
uvicorn==0.51.0
python-multipart==0.0.32
faster-whisper==1.2.1
sentence-transformers==5.6.0
rapidfuzz==3.14.5
smartcut==1.7
imageio-ffmpeg==0.6.0
# Optional — only needed for the "✦ Cut with AI" button (needs a C compiler or a
# prebuilt wheel for your platform). Everything else works without it:
# llama-cpp-python==0.3.33
